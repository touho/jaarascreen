let bot = require('./src/jaarascreenbot');
let server = require('./src/server');

console.log(`JaaraScreen started
###################

Usage:
/jaarascreen <message>
Or just send a photo into a channel where JaaraScreen is.

`);

